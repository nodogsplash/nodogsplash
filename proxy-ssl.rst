ProxySSL Option
===============

Values: Boolean - 1 / 0
Default: 0

The ProxySSL Option is used to enable/disable DNAT the https request to the gateway.
This help to terminate https connections, for example on mobile phones.
